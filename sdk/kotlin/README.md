# Kotlin contract API

This module provides small model-neutral data types, a transport interface, and a client that adds an opaque request ID. It has no Android, inference-runtime, or production SARA dependency.

```bash
./gradlew test
```
